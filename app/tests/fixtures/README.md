`red-exclamation.png` is a visually reviewed generic warning icon extracted from
an example Word export. It contains no patient text; its only PNG metadata is
image resolution. Tests use it to check exact pixel recognition, lossless
re-encoding, and rejection of modified images. The source journal is not included.
