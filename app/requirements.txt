# The app is Python-standard-library only, with one optional exception:
# PyMuPDF renders PDF pages for the vision model and reads PDF text layers.
# Without it the app still runs; PDF uploads show an install hint instead.
pymupdf>=1.24
